<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
          "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
          "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
    Checkstyle configuration for this project.
    Based on Google Java Style Guide with project-specific adjustments:
    - Max line length: 120 (relaxed from Google's 100)
    - Method names may contain underscores (test naming convention)
    - Static star imports allowed (JUnit Assertions pattern)
-->
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- File-level checks -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <module name="TreeWalker">

        <!-- Naming conventions -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="TypeName"/>
        <module name="ConstantName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>

        <!-- Allow underscores in method names for test naming (given_when_then) -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9_]*$"/>
        </module>

        <!-- Braces — every if/for/while block must use {} -->
        <module name="NeedBraces"/>

        <!-- Imports -->
        <module name="AvoidStarImport">
            <!-- static star imports are allowed (e.g. import static Assertions.*) -->
            <property name="allowStaticMemberImports" value="true"/>
        </module>
        <module name="UnusedImports"/>

        <!-- Code quality -->
        <module name="EqualsHashCode"/>
        <module name="ModifierOrder"/>
        <module name="EmptyBlock"/>

    </module>
</module>